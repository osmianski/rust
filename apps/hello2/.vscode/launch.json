{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug",
            "program": "${workspaceRoot}/target/debug/hello2",
            "args": ["serve"],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "build"
        }
    ]
}