{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug",
            "program": "${workspaceRoot}/target/debug/app",
            "args": ["serve"],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "build"
        }
    ]
}